"use strict"; // treat all js code as newer version
// premitive datatype
let amount = 45 // number contain both int and deciaml type
let name = "saurabh" // string
let hasDone = true // boolean type
let extraAmount = null // null is a standalone value
let subject; // undefined value 
// Symobol -> unique
// bigInt
// object

let obj = {
    name:"saurabh",
    occ: "tech"
}
//array
let a = [1,2,3,4]

//function
function add( a ,b){
    return a + b;
}

console.table([typeof amount , typeof name , typeof hasDone , typeof extraAmount , typeof subject]);